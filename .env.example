# Copy this file to .env and fill in ONE of these to enable the LLM planner/explainer.
# If none are set, the agent runs fully offline with a deterministic rule-based
# planner and template-based explainer -- the whole app works with zero keys.

GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
